To successfully use index.py, we first need to break TIME.ALL into smaller docs.

To do so, simply create a directory called 'doc', and run SplitFile.py first.

SplitFile will store the broken documents as seperate docs in  the newly created 
directory.
